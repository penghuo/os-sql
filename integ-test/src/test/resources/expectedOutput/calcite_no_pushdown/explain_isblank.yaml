calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(account_number=[$0], firstname=[$1], address=[$2], balance=[$3], gender=[$4], city=[$5], employer=[$6], state=[$7], age=[$8], email=[$9], lastname=[$10])
        LogicalFilter(condition=[OR(IS NULL($1), =(CHAR_LENGTH(TRIM(FLAG(BOTH), ' ', $1)), 0))])
          CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..17=[{inputs}], expr#18=[IS NULL($t1)], expr#19=[FLAG(BOTH)], expr#20=[' '], expr#21=[TRIM($t19, $t20, $t1)], expr#22=[CHAR_LENGTH($t21)], expr#23=[0], expr#24=[=($t22, $t23)], expr#25=[OR($t18, $t24)], proj#0..10=[{exprs}], $condition=[$t25])
        CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
