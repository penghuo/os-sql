calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(firstname=[$1], age=[$8])
        LogicalFilter(condition=[AND(=($1, 'Amber'), =(-($8, 2), 30))])
          CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..17=[{inputs}], expr#18=['Amber':VARCHAR], expr#19=[=($t1, $t18)], expr#20=[2], expr#21=[-($t8, $t20)], expr#22=[30:BIGINT], expr#23=[=($t21, $t22)], expr#24=[AND($t19, $t23)], firstname=[$t1], age=[$t8], $condition=[$t24])
        CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
