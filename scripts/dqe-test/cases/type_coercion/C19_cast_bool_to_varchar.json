{
  "id": "C19",
  "name": "cast_bool_to_varchar",
  "query": "SELECT CAST(is_active AS VARCHAR) AS result FROM dqe_test_all_types WHERE is_active IS NOT NULL LIMIT 5",
  "expected": {
    "schema": [
      {
        "name": "result",
        "type": "VARCHAR"
      }
    ]
  },
  "ignore_order": true
}
