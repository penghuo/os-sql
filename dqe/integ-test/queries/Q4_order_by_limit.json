{
  "id": "Q4",
  "name": "ORDER BY DESC with LIMIT",
  "endpoint": "/_plugins/_trino_sql",
  "query": "SELECT category, status FROM dqe_test_logs ORDER BY status DESC LIMIT 3",
  "expected": {
    "status": 200,
    "schema": [
      {"name": "category", "type": "keyword"},
      {"name": "status",   "type": "long"}
    ],
    "total": 3
  },
  "note": "Only checks total and schema. Exact row ordering across shards is non-deterministic with distributed LIMIT."
}
