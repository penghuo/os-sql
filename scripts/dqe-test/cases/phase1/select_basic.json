{
  "id": "P001",
  "name": "basic_select_with_limit",
  "description": "SELECT specific columns with ORDER BY and LIMIT from employees",
  "query": "SELECT id, name, department FROM dqe_test_employees ORDER BY id LIMIT 3",
  "expected": {
    "schema": [
      {"name": "id", "type": "INTEGER"},
      {"name": "name", "type": "VARCHAR"},
      {"name": "department", "type": "VARCHAR"}
    ],
    "data": [
      [1, "Alice Smith", "Engineering"],
      [2, "Bob Johnson", "Marketing"],
      [3, "Carol Williams", "Engineering"]
    ]
  }
}
