{
  "id": "P005",
  "name": "select_with_boolean_filter",
  "description": "Filter on boolean field and verify boolean values in result",
  "query": "SELECT id, name, active FROM dqe_test_employees WHERE active = true ORDER BY id",
  "expected": {
    "schema": [
      {"name": "id", "type": "INTEGER"},
      {"name": "name", "type": "VARCHAR"},
      {"name": "active", "type": "BOOLEAN"}
    ],
    "data": [
      [1, "Alice Smith", true],
      [2, "Bob Johnson", true],
      [4, "David Brown", true],
      [5, "Eve Davis", true]
    ]
  }
}
