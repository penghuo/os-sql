{
  "id": "C22",
  "name": "cast_scaled_to_double",
  "query": "SELECT CAST(price_scaled AS DOUBLE) AS result FROM dqe_test_all_types WHERE price_scaled IS NOT NULL LIMIT 5",
  "expected": {
    "schema": [
      {
        "name": "result",
        "type": "DOUBLE"
      }
    ]
  },
  "ignore_order": true,
  "float_tolerance": 0.01
}
