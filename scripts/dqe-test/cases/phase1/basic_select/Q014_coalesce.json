{
  "id": "Q014",
  "name": "coalesce",
  "query": "SELECT id, COALESCE(str_val, 'default') FROM dqe_test_nulls LIMIT 10",
  "expected": {
    "schema": [
      {
        "name": "id",
        "type": "VARCHAR"
      },
      {
        "name": "COALESCE(str_val, 'default')",
        "type": "VARCHAR"
      }
    ],
    "expected_row_count": 10
  },
  "ignore_order": true
}
