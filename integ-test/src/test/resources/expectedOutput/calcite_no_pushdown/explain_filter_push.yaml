calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(age=[$8])
        LogicalFilter(condition=[>($3, 10000)])
          LogicalFilter(condition=[<($8, 40)])
            LogicalFilter(condition=[>($8, 30)])
              CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..17=[{inputs}], expr#18=[Sarg[(30L:BIGINT..40L:BIGINT)]:BIGINT], expr#19=[SEARCH($t8, $t18)], expr#20=[10000:BIGINT], expr#21=[>($t3, $t20)], expr#22=[AND($t19, $t21)], age=[$t8], $condition=[$t22])
        CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
