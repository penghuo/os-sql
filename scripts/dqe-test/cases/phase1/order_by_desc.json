{
  "id": "P003",
  "name": "order_by_descending",
  "description": "ORDER BY with DESC direction",
  "query": "SELECT id, name FROM dqe_test_employees ORDER BY id DESC LIMIT 2",
  "expected": {
    "schema": [
      {"name": "id", "type": "INTEGER"},
      {"name": "name", "type": "VARCHAR"}
    ],
    "data": [
      [5, "Eve Davis"],
      [4, "David Brown"]
    ]
  }
}
