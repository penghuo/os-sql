calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(account_number=[$0], firstname=[$1], address=[$2], balance=[$3], gender=[$4], city=[$5], employer=[$6], state=[$7], age=[$8], email=[$9], lastname=[$10], distinct_states=[$11])
        LogicalSort(sort0=[$12], dir0=[ASC])
          LogicalProject(account_number=[$0], firstname=[$1], address=[$2], balance=[$3], gender=[$4], city=[$5], employer=[$6], state=[$7], age=[$8], email=[$9], lastname=[$10], distinct_states=[DISTINCT_COUNT_APPROX($7) OVER (ORDER BY $18 NULLS LAST ROWS UNBOUNDED PRECEDING)], __stream_seq__=[$18])
            LogicalProject(account_number=[$0], firstname=[$1], address=[$2], balance=[$3], gender=[$4], city=[$5], employer=[$6], state=[$7], age=[$8], email=[$9], lastname=[$10], _id=[$11], _index=[$12], _score=[$13], _maxscore=[$14], _sort=[$15], _routing=[$16], _highlight=[$17], __win_0=[ROW_NUMBER() OVER ()])
              CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableCalc(expr#0..19=[{inputs}], proj#0..10=[{exprs}], distinct_states=[$t19])
      EnumerableLimit(fetch=[10000])
        EnumerableSort(sort0=[$18], dir0=[ASC])
          EnumerableWindow(window#0=[window(order by [18] rows between UNBOUNDED PRECEDING and CURRENT ROW aggs [DISTINCT_COUNT_APPROX($7)])])
            EnumerableWindow(window#0=[window(rows between UNBOUNDED PRECEDING and CURRENT ROW aggs [ROW_NUMBER()])])
              CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
