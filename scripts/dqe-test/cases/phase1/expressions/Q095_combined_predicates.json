{
  "id": "Q095",
  "name": "combined_predicates",
  "query": "SELECT id, count_int FROM dqe_test_all_types WHERE count_int BETWEEN 10 AND 100 AND status IN ('active', 'pending') ORDER BY count_int DESC LIMIT 20",
  "expected": {
    "schema": [
      {
        "name": "id",
        "type": "VARCHAR"
      },
      {
        "name": "count_int",
        "type": "INTEGER"
      }
    ],
    "expected_min_row_count": 1
  }
}
